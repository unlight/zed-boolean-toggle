[workspace]
members = [
    "extension",
    "server",
]
resolver = "2"

# Optional: Optimize the whole workspace for release builds
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
